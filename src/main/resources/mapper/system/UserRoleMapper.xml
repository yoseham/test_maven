<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.szu.bigdata.rsp_platform.system.dao.UserRoleMapper">

    <select id="selectByUserIds" resultType="cn.edu.szu.bigdata.rsp_platform.system.model.UserRole">
        SELECT a.*,b.role_name FROM sys_user_role a
        LEFT JOIN sys_role b ON a.role_id=b.role_id
        WHERE a.status=0 and b.status=0 and a.user_id IN
        <foreach collection="userIds" open="(" close=")" separator="," item="userId">
            #{userId}
        </foreach>
    </select>

    <insert id="insertBatch">
        INSERT INTO sys_user_role(user_id,role_id) VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId},#{roleId})
        </foreach>
    </insert>

    <delete id="deleteTrash">
        DELETE FROM sys_user_role  WHERE (status=1) OR (user_id  NOT IN (SELECT user_id FROM sys_user where sys_user.status=0)) OR (role_id NOT IN (SELECT role_id FROM sys_role where sys_role.status=0))
    </delete>
</mapper>